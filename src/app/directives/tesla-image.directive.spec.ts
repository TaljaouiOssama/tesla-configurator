import { TeslaImageDirective } from './tesla-image.directive';

describe('TeslaImageDirective', () => {
  it('should create an instance', () => {
    const directive = new TeslaImageDirective();
    expect(directive).toBeTruthy();
  });
});
